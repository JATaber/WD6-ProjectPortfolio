<div class="jumbotron">
    {{# if products }}
        <div class="row">
            <div class="col-sm-6 col-md-6 col-md-offset-3 col-sm-offset-3">
                <ul class="list-group">
                    {{# each products }}
                        <li class="list-group-item">
                            <span class="badge badge-secondary">{{ this.qty }}</span>
                            <strong>{{ this.item.title }}</strong>
                            <span class="label label-success">${{ this.price }} </span>
                            <div class="btn-group">
                                <button type="button" class="btn btn-primary btn-sm dropdown-toggle" data-toggle="dropdown">
                                    Action <span class="caret"></span>
                                </button>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" href="/removeWish/{{this.item._id}}"><i class="far fa-trash-alt"></i> Remove</a>
                                    <a class="dropdown-item" href="/shoppingCart/{{this.item._id}}">Add to Cart</a>
                                    <!--<a class="dropdown-item" href="/remove/{{this.item._id}}">Remove All</a>-->
                                </div>
                            </div>
                        </li>
                    {{/each}}

                </ul>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-6 col-md-6 col-md-offset-3 col-sm-offset-3">
                <strong>Total: {{ totalPrice }}</strong>
            </div>
        </div>
    {{else}}
        <div class="row">
            <div class="col-sm-6 col-md-6 col-md-offset-3 col-sm-offset-3">
                <h2>No Items in Your Wishlist</h2>
            </div>
        </div>
    {{/if}}
</div>